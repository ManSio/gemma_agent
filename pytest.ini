[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test* *Tests *TestCase
python_functions = test_*
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: live or heavy integration (may need network)
    slow: slow tests (skipped in quick CI subsets)
