# Core / Telegram
aiogram>=3.28.2
python-dotenv>=1.2.2
pydantic>=2.13.4
# IANA tz on Windows (zoneinfo); on Linux optional if system tzdata present
tzdata>=2024.2

# HTTP / IO
requests>=2.31.0
# aiogram 3.28.x требует aiohttp>=3.9.0,<3.14 (не 3.14+ — иначе ResolutionImpossible в CI)
aiohttp>=3.9.0,<3.14
beautifulsoup4>=4.15.0

# Host metrics (CPU / RAM / disk) — core/host_resources.py
psutil>=7.2.2

# API (api.py, docker profile api)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# DB / ORM (core/database.py)
sqlalchemy>=2.0.50

# Security (core/security.py)
cryptography>=48.0.0

# HTML sanitization (core/telegram_util.py)
bleach>=6.3.0

# Documents (core/document_intake.py — PDF, DOCX, PPTX, XLSX, YAML, …)
pypdf>=6.12.1
pymupdf>=1.27.2.3
python-docx>=1.2.0
python-pptx>=1.0.2
openpyxl>=3.1.5
PyYAML>=6.0.3

# Imaging (modules/imaging/image_tools.py)
Pillow>=12.2.0

# Vector RAG (modules/books_rag + Qdrant; опционально, заменено на core/qdrant_http.py)
# qdrant-client>=1.7.0  # удалён — использует numpy, несовместим с AMD C-50

# Optional — раскомментируйте при необходимости
# openai>=1.0.0
# langchain

# Локальный STT (VOICE_STT_BACKEND=vosk); без пакета в контейнере будет ModuleNotFoundError
vosk>=0.3.45
# psycopg2-binary  # PostgreSQL вместо SQLite
