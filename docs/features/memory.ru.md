# Память (Mem0)

| Режим | Когда |
|-------|--------|
| **Stub** (bootstrap) | Dev, малый круг |
| **Local server** | Прод на LAN |
| **Облако** | api.mem0.ai |

## Stub — ограничения

JSON на диске, поиск по подстроке, без эмбеддингов. Для теста ок, не для мультитенанта.

## Старт

```bash
bash scripts/gemma_panel.sh mem0-start
```

## В боте

Модуль `memory`, префикс API `v3`, слэши `/mem_*`.
